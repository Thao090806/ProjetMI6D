#ifndef FIN_JEU_O
#define FIN_JEU_O

#include "../include/parametres.h"

void fin_jeu(Combattants combattants[], Equipes equipe[], Equipes equipe_ennemie[]) {
    free(combattants);
    free(equipe);
    free(equipe_ennemie); 
}

#endif
